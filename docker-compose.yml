version: '3.8'
services:
  yui_r:
    build: .
    container_name: yui_r_bot
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    command: python main.py
